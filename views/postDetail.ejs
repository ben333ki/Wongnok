<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.topic %></title>
</head>
<body>
    <h1><%= post.topic %></h1>
    <img src="<%= post.image %>" alt="Post Image" width="500">
    <p>Posted by: <%= post.createdBy.name %></p>

    <% if (isOwner) { %>
        <!-- Show Edit and Delete buttons only if the user owns the post -->
        <form action="/posts/<%= post._id %>/delete" method="POST" style="display:inline;">
            <button type="submit">Delete</button>
        </form>
        <a href="/posts/<%= post._id %>/edit"><button>Edit</button></a>
    <% } %>
    
    <a href="/posts">Back to Posts</a>
</body>
</html>
